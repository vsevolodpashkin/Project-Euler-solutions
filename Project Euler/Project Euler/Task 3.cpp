#include<vector>
#include<iostream>
#include"Main Functions.h"

/*int main()
{
	std::vector<long long> vec{0};
	auto s = 600851475143;
	auto result{ 0 };
	auto start = static_cast<long long>(sqrt(s));

	

	for (auto it = vec.rend(); it != vec.rbegin(); it--)
	{
		if (s% *it == 0)
		{
			result = s;
			break;
		}
	}

	std::cout << "Maximum prime factor of 600851475143 is " << start << std::endl;
	return 0;
}
*/

